/* === VIVENTIUM START === Thin legacy adapter for typed callback sanitization. === */
const { isSafeGlassHiveActionUrl, sanitizeGlassHiveCallbackText } = require('@librechat/api');

module.exports = { isSafeGlassHiveActionUrl, sanitizeGlassHiveCallbackText };
/* === VIVENTIUM END === */
