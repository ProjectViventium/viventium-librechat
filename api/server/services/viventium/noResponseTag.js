/* === VIVENTIUM START === Thin legacy adapter for typed no-response normalization. === */
const {
  NO_RESPONSE_TAG,
  isNoResponseOnly,
  isNoResponseTag,
  normalizeNoResponseText,
  stripTrailingNTA,
} = require('@librechat/api');

module.exports = {
  NO_RESPONSE_TAG,
  isNoResponseOnly,
  isNoResponseTag,
  normalizeNoResponseText,
  stripTrailingNTA,
};
/* === VIVENTIUM END === */
