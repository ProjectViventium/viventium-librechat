/* === VIVENTIUM START === Preserve legacy imports through the shared compiled prompt owner. === */
'use strict';

const {
  PROMPT_BUNDLE_ENV,
  KNOWN_RUNTIME_PLACEHOLDERS,
  getPromptBundleStatus,
  getPromptMetadata,
  getRequiredPromptText,
  getPromptText,
  resetPromptRegistryForTests,
} = require('@librechat/api');

module.exports = {
  PROMPT_BUNDLE_ENV,
  KNOWN_RUNTIME_PLACEHOLDERS,
  getPromptBundleStatus,
  getPromptMetadata,
  getRequiredPromptText,
  getPromptText,
  resetPromptRegistryForTests,
};
